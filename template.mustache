<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<meta name=viewport content="width=device-width, initial-scale=1">
<style>
body, div {
	margin: 0;
	border: 0;
	padding: 0;

	font-family: Helvetica, sans-serif;
}

.story {
	margin-bottom: 0.75em;
	border-bottom: 1px #eee solid;
	padding-bottom:0.25em;
	overflow:hidden;
}

.tweet-profile-image {
	float:left;
	/*margin-right:0.5em;*/
}

.tweet-text-block {
	margin-left:60px;
	margin-right: 10px;
}

.tweet-screen-name {
	font-style: italic;
}

</style>
</head>
<body>
	{{#top}}
	<div class="story">
	<div  class="tweet-profile-image">
	<a href="https://twitter.com/{{top_tweet.screen_name}}/statuses/{{top_tweet.tweet_id}}">
	<img src="{{top_tweet.profile_image_url}}" width=48 height=48>
	</a>
	</div>
	<div class="tweet-text-block"><span class="tweet-screen-name">@{{top_tweet.screen_name}}</span> {{{top_tweet.expanded_text}}}</div>
	</div>
	{{/top}}
	<div class="meta">Last Updated: <span id="last-updated" data-timestamp="{{meta.last_updated}}">{{meta.last_updated}}</span></div>
</body>
</html>